const titleDiv = document.getElementById('golden_book_title');

window.addEventListener('load', () => {
    titleDiv.parentElement.classList.add('goldenBookFormPublic');
})

const textDiv = document.getElementById('golden_book_text');

window.addEventListener('load', () => {
    textDiv.parentElement.classList.add('goldenBookFormPublic');
})