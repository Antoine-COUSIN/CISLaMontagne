<form method="post" action="{{ path('amicale_descript_delete', {'id': amicale_descript.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amicale_descript.id) }}">
    <button class="btn">Delete</button>
</form>
