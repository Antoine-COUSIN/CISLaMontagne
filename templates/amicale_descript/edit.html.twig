{% extends 'base.html.twig' %}

{% block title %}Edit AmicaleDescript{% endblock %}

{% block body %}
    <section id="editStationCorePicture" class="editContainer">
        <div class="container">
            <h2>Mettre à jour l'image de groupe</h2>

            {{ include('amicale_descript/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <a class="btn-back" href="{{ path('amicale_descript_index') }}">Retour à la liste</a>
        </div>
    </section>  
{% endblock %}
