<form method="post" action="{{ path('role_center_delete', {'id': role_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role_center.id) }}">
    <button class="btn">Delete</button>
</form>
