{% extends 'base.html.twig' %}

{% block title %}PharmaStock{% endblock %}

{% block body %}
    <h1>PharmaStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pharma_stock.id }}</td>
            </tr>
            <tr>
                <th>ProductsName</th>
                <td>{{ pharma_stock.productsName }}</td>
            </tr>
            <tr>
                <th>ProductsQuantity</th>
                <td>{{ pharma_stock.productsQuantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pharma_stock_index') }}">back to list</a>

    <a href="{{ path('pharma_stock_edit', {'id': pharma_stock.id}) }}">edit</a>

    {{ include('pharma_stock/_delete_form.html.twig') }}
{% endblock %}
