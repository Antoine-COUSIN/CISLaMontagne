<form method="post" action="{{ path('pharma_stock_delete', {'id': pharma_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharma_stock.id) }}">
    <button class="btn">Delete</button>
</form>
