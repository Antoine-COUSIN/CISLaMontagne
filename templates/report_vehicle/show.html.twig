{% extends 'base.html.twig' %}

{% block title %}ReportVehicle{% endblock %}

{% block body %}
    <h1>ReportVehicle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report_vehicle.id }}</td>
            </tr>
            <tr>
                <th>ReportVehicle</th>
                <td>{{ report_vehicle.reportVehicle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('report_vehicle_index') }}">back to list</a>

    <a href="{{ path('report_vehicle_edit', {'id': report_vehicle.id}) }}">edit</a>

    {{ include('report_vehicle/_delete_form.html.twig') }}
{% endblock %}
