<form method="post" action="{{ path('amicale_news_delete', {'id': amicale_news.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amicale_news.id) }}">
    <button class="btn_delete"><i class="fas fa-trash"></i>Supprimer</button>
</form>
