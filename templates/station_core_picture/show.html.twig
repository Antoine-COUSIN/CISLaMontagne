{% extends 'base.html.twig' %}

{% block title %}StationCorePicture{% endblock %}

{% block body %}
    <h1>StationCorePicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ station_core_picture.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ station_core_picture.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('station_core_picture_index') }}">back to list</a>

    <a href="{{ path('station_core_picture_edit', {'id': station_core_picture.id}) }}">edit</a>

    {{ include('station_core_picture/_delete_form.html.twig') }}
{% endblock %}
