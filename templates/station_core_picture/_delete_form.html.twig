<form method="post" action="{{ path('station_core_picture_delete', {'id': station_core_picture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ station_core_picture.id) }}">
    <button class="btn">Delete</button>
</form>
