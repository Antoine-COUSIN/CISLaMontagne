{% extends 'base.html.twig' %}

{% block title %}New ChiefSpeech{% endblock %}

{% block body %}
<section id="newStationCorePicture" class="newContainer">
        <div class="container">
            <h1>Créer l'encart "mot du chef de centre"</h1>

            {{ include('chief_speech/_form.html.twig') }}

            <a class="btn-back" href="{{ path('chief_speech_index') }}">Retour à la liste</a>
        </div>
    </section> 

{% endblock %}
