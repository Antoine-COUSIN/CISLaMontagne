<form method="post" action="{{ path('order_required_delete', {'id': order_required.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_required.id) }}">
    <button class="btn">Supprimer</button>
</form>
