{% extends 'base.html.twig' %}

{% block title %}OrderRequired{% endblock %}

{% block body %}
    <h1>OrderRequired</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_required.id }}</td>
            </tr>
            <tr>
                <th>OrderDescript</th>
                <td>{{ order_required.orderDescript }}</td>
            </tr>
            <tr>
                <th>OrderDate</th>
                <td>{{ order_required.orderDate ? order_required.orderDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_required_index') }}">back to list</a>

    <a href="{{ path('order_required_edit', {'id': order_required.id}) }}">edit</a>

    {{ include('order_required/_delete_form.html.twig') }}
{% endblock %}
